<configuration>
    <property>
        <name>fs.default.name</name>
        <!-- if defined as localhost, spark-slave can't connect to it-->
        <value>hdfs://spark-cluster-master:9000</value>
    </property> 

</configuration>
